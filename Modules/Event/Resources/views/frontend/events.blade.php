    <!-- Courses area S t a r t-->
    <section class="ot-discount-courses-area section-padding section-data-load"
        data-url="{{ route('event.home.ajax.event.list') }}"
        @if (@$section->color) style="background:{{ @$section->color }}" @endif>
        <!-- get data from featuredCourses() -->
    </section>
    <!-- End-Courses area S t a r t-->
